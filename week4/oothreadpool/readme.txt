cpp的makefile文件的编写入门版：

CC：=（这玩意的名字也能修改）
CPPFLAGS：=（可以修改名称）
LIBS：=（不能修改名称）
SRCS:=
OBJS:=
TARGETS:=
$(TARGETS):$(OBJS)
    编译的语句。用通配符代替
%.o:%.cc
    编译的语句
clean:
      rm -rf $() $()



总的来说，感觉makefile文件很像一个脚本文件，也很像之前的c语言链接数据库，
都是将命令行放在文件中，然后固定的名称用专门的的单词代替，没有代码那么规矩
只需要与命令行相似即可
